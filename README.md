# Hello, Sera Android prototype

A minimal native Android prototype. Tapping **편지가 왔어요!** immediately plays the bundled Sera voice file without leaving the screen.

## Build

GitHub Actions builds a debug APK automatically on pushes to `main`.

Output artifact: `hello-sera-apk`
